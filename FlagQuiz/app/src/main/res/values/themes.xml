<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FlagQuiz" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/my_bg</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/hm</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/pink</item>
        <item name="android:windowSplashScreenBrandingImage">@drawable/branded</item>
    </style>

    <style name="Theme.FlagQuiz" parent="Base.Theme.FlagQuiz" />
</resources>